extends layout

block main

  form(action="/bets/" method="post")
    dt
      - let ordering = game.createOrdering()
      h1= game.currentHand
      h1(class="enteringBets") #{ordering[0]} goes first! #{ordering[ordering.length-1]} goes last! 
      each player, i in ordering
        dl
          label(for=`${player}`) Player #{player} enter bet
        dd(class=(invalidBet && i === (ordering.length - 1))?"invalidBet":"")
          input(type="text"
            name=`${player}`
            placeholder="6"
            value=bets?bets[player]:"")
  
  


    fieldset.actions
      input(type="submit" value="Enter bets")
